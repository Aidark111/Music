import UIKit

struct SizeConstant {
    static let miniPlayer: CGFloat = 70  // height for the mini player view
    static let fullPlayer: CGFloat = UIScreen.main.bounds.height + 250   // height for full
}
